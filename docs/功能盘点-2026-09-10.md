# 功能完善度盘点

> 盘点日期：2026-09-10　方式：**代码级交叉核对**（文档声称 vs 实际实现），非文档转抄。
> 基准：[README 待办事项](../README.md#待办事项backlog)（2026-09-07 盘点）、[ARCHITECTURE §10](ARCHITECTURE.md#10-已知取舍与缺口)、PRD v0.2 §5.5。
> 测试现状：common 20 / manager 68 / executor 84 = **172 全绿**。

## 结论先行

1. **台账有 5 处与实际代码不符**，其中 3 处会直接导致排期错误（把未实现的当已完成、把已实现的当未完成、把已有端点的当待核实）。**这 5 处只需改文档，不改代码。**
2. **真实功能缺口：P1 六项、P2 六项。**
3. **另有 6 处「半成品」——读写两端不对称**：一侧实现完整、另一侧完全空白，最容易在演示时被当成"已经能做"。参见 §3。

---

## 0. 修复进展（滚动更新）

> 报告正文保留 2026-09-10 的原始盘点结论（它是判断依据，不该被后续修改污染）。
> 本节只记录「哪几条已闭环」，便于对照排期。

| 批次 | 范围 | 状态 | 落点 |
| --- | --- | --- | --- |
| T45 | §1 的 5 处文档与代码不符 | ✅ 已修订 | README / ARCHITECTURE §5.1 §10 §11 / API §1.4 §2.5 |
| T46 | §1 第 1 条：WEIGHTED 权重写入链路 | ✅ 端到端 | `UpstreamEntryRequest.weights` + `ServerService.requireWeights` + UI 逐行权重编辑器 + `UpstreamWeightsValidationTest`(9) |
| T47 | §1 第 2 条：tool 级 Auth-B 覆盖端到端 | ✅ 端到端 | `AuthConfig*` 三维度判定修正 + `saveToolAuthB` 系列 + `ToolView.authB` + UI「鉴权覆盖」列与弹窗分区 + `AuthConfigScopeTest`(5) |
| T48 | §3 半成品：resources / prompts 管理 | ✅ 端到端 | `V7__resource_prompt.sql` + `McpResource`/`McpPrompt` + `ResourcePromptService` + `ServerAccessGuard` + `PromptTemplate`(common) + 8 个端点 + `ResourcePromptPanel.vue` + 4 处测试 |
| T49 | 剩余半成品 + P2 项 | ✅ 已完成 | 见下 |

**T49 落点**：

| 子项 | 落点 |
| --- | --- |
| 审计 append-only | `V8__audit_append_only.sql`：`BEFORE UPDATE/DELETE` 触发器（对属主同样生效）+ `REVOKE UPDATE, DELETE, TRUNCATE`。**刻意不给 TRUNCATE 设触发器**：那是"删库"而非"改历史"，目标不扩大 |
| 审计导出 | `Csv`（common，RFC 4180 + 公式注入防护 + BOM）+ `AuditService.exportCsv`（复用列表页的部门隔离，逐页取，超上限抛错不截断）+ `GET /audits/export`（`audit:read`，导出动作本身留痕 `audit.export`）+ `AuditView` 导出按钮 + `downloadCsv`（blob 错误还原） |
| 死字段 `ExecutorCluster.scopes` | **改造为可读可用的发布配额**：V9 改名 `quota` + `ClusterQuota` 值对象 + `PublishService` 发布时强制 + `ClusterView`/控制台读写。判定抽成纯函数 `ClusterQuota.violations(...)` |
| 限流 | **评估后不做**（`ARCHITECTURE §4.6` 有配额 vs 限流的对比表）。理由是速率限制需要令牌桶 + 协议级拒绝语义 + 快照字段，属独立 P1 设计项，不是"给字段加读取方" |
| OPS-01 / OPS-02 文档 | `API.md §4.5` 补业务指标清单（16 个指标名 + 标签 + 语义）与出站 `traceparent` 契约 |

**T49 顺带发现并修复的真实缺陷**：`ClusterService.update` 里 `description` 的修改**完全不写审计**
（既不在 `changes` 里，也没有单独记录）。已并入 `changes`，同时把「审计口径」与「revision 口径」
拆成两个变量——否则新增的 quota 变更会连带给全网节点造成一次无意义的快照重载。

**T45–T48 顺带发现并修复的真实缺陷**（原盘点未列出）：

1. **`serverLevelConfig()` 可能命中多行 → 发布链路直接抛异常。** V5 把 REST 服务级 Auth-B 也存成
   `tool_id = 0`，而 Server 级查询只按 `(serverId, 0)` 查。同一 Server 下挂 ≥2 个 REST 服务且
   各配了 Auth-B 时，`Optional` 会拿到两行 → `IncorrectResultSizeDataAccessException`。
   修法：查询一律带 `UpstreamServiceIdIsNull`，并用 `AuthConfigScopeTest` 的
   「三维度互斥且穷尽」不变量钉死。
2. **前端审计动作码表缺 7 个 code**（`access.*` 4 个、`server.delete`、`catalog.*` 2 个），
   审计页会显示原始 code。已补齐至与后端 37 个一致。
3. **`docs/API.md` 的 Auth-B 示例用了不存在的类型 `STATIC_BEARER`**（那是 Auth-D 的模式名）。
   已改为 `{"type":"HTTP","scheme":"bearer","secret":"…"}`。

> 测试基线：原盘点 172 全绿 → T45–T48 后 **common 27 / manager 94 / executor 84 = 205 全绿**
> → T49 后 **common 42 / manager 111 / executor 103 = 256 全绿**（新增 `CsvTest` 6 例、
> `ClusterQuotaTest` 11 例、`AuditExportTest` 6 例，以及 OPS-01/OPS-02 的 25 例）。

> **未能在本机验证的一项**：V8 / V9 两条迁移需要真实 PostgreSQL 才能执行（本机 docker 拉不到
> `postgres:16-alpine` 镜像）。语法与语义已逐条自查，但**首次部署时必须确认 migration 成功**
> —— Flyway 失败会直接阻止启动，不会静默。重点确认三件事：
> ① `audit_log` 上出现 `trg_audit_log_no_update` / `trg_audit_log_no_delete`；
> ② 手工 `UPDATE audit_log SET action='x' WHERE id=1;` 被拒绝（`restrict_violation`）；
> ③ `executor_cluster.quota` 列存在且 Hibernate `ddl-auto: validate` 通过。

---

## 1. 文档与代码不符（P0：先修文档，否则排期会错）

| # | 位置 | 文档怎么说 | 代码实际 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | README「当前范围」 | WEIGHTED「权重**已可落库**（`server_upstream.weights` JSONB）且 Executor 按权分发，剩余缺口是控制台权重编辑器」 | `setWeights` **全仓零调用**；`ServerService.upsertUpstream` 构造 `UpstreamSnapshot` 时**硬编码 `List.of()`**。读取侧 `readIntegerList(u.getWeights())` 在，但没有写入路径 → 该列恒为 null | ❌ README 错，[ARCHITECTURE §10](ARCHITECTURE.md#10-已知取舍与缺口) 的「weights 恒为空数组」**才是对的**。缺口不只是 UI，还缺 API 字段 + 落库 |
| 2 | README「提前完成（勿重复排期）」 | 「tool 级 Auth-B 覆盖（`authBOverride`）」列为**已完成** | DB 唯一索引（V5）在、`ToolSnapshot.authBOverride` 字段在、executor `UpstreamCredentialProvider` 会读——但 ①`AuthConfigService` **无 `saveToolAuthB`** ②`ToolOverlayRequest` **无该字段** ③`OverlayService.toSnapshot` **传 `null`** ④UI 无入口 → **端到端未实现**，永远回落 REST 服务级 | ❌ README 错，ARCHITECTURE §10「未实现」对；但其描述「Auth-B **只在 Server 级**」已过时（现为 REST 服务级） |
| 3 | README Backlog OPS-01 | 「`/metrics` 端点**未见实现痕迹（待核实）**」 | `mcp-executor/pom.xml` 有 `micrometer-registry-prometheus`；`application.yml` `include: health,info,metrics,prometheus` → **端点存在** | ⚠️ 可销案。真实缺口是**零业务自定义指标**（见 §2 P1-6） |
| 4 | README Backlog REG-03 | 「待收尾：**re-import diff 报告**」 | **已实现**：`RegistrationService.reimport()` + `DiffReport`（added/removed/changed/suspended）+ `POST /registrations/{id}/reimport` 与 `/reimport-upload` + UI `reimportUpload` | ⚠️ 该行可删，真实剩余只有「文档多版本历史链」 |
| 5 | [ARCHITECTURE §11](ARCHITECTURE.md#11-可观测性) 指标表 | 「Prometheus 指标：Executor `/actuator/prometheus`（**需 `metrics:read`**）」 | `mcp-executor` **没有 Spring Security**（pom 无依赖），**无从校验 `metrics:read`**。该权限是 manager `SecurityConfig` 的规则 | ❌ 表述错误。executor 的 `/actuator/**`、`/executor/status`、`/healthz` **全部无应用层鉴权**，靠网络隔离兜底——这是刻意设计，但文档把它写得像有 RBAC 保护 |

> 附带：ARCHITECTURE §10 称「`UpstreamRequest` 没有 weights 字段」，实际类名是 `RestRequest`（`UpstreamRequest` 不存在）。

---

## 2. 真实功能缺口

### P1（GA 必达候选）

| 编号 | 缺口 | 读端（executor） | 写端（manager） | 一句话 |
| --- | --- | --- | --- | --- |
| **SVR-05/06** | Resource / Prompt 管理 | ✅ **100%**：`resources/list`·`resources/read`·`prompts/list`·`prompts/get`，含静态内容、tool 映射、`{{arg}}` 模板渲染、必填参数校验、capabilities 门控、`ttlMs` | ❌ **0%**：`SnapshotAssembler.build()` 硬编码 `List.of(), List.of()`；无管理模型、无 UI、无组装 | 读端做得非常完整，写端完全空白 → **对外恒空清单**。这是"看起来能跑但永远没数据"的典型 |
| **EXE-07** | Auth-D OAuth 2.1 | 元数据字段会存、`resourceMetadataUrl` 会派生 | 授权码 + PKCE + DCR 未实现；配置成 OAUTH2 的端点**显式 501 拒绝** | 半实现的鉴权比没有更危险，故显式拒绝——这个决策是对的 |
| **EXE-04/B** | WEIGHTED 权重写入 | ✅ 按权分发完整，权重数量不匹配退回轮询 + 告警 + 单测 | ❌ 无 API 字段、无落库、无 UI（见 §1-1） | executor 侧是完工的，只差把权重喂进去 |
| **BR-4** | tool 级 Auth-B 覆盖 | ✅ `UpstreamCredentialProvider` 三层优先级解析已实现 | ❌ 无 API / DTO / 组装 / UI（见 §1-2） | 同 WEIGHTED：执行侧完工，配置侧空白 |
| **PUB-05** | 发布可观测看板 | Executor 有 `/executor/status` 与熔断状态 | ❌ `DashboardView` 只有 4 个计数（`servers`/`registrations`/`clusters`/`onlineNodes`），**无健康、无调用量、无错误率、无延迟** | 现有"概览"是导航页，不是看板 |
| **OPS-01** | 业务指标 | 端点就位（自动 JVM/HTTP 指标） | ❌ 全仓**零** `MeterRegistry`/`Counter`/`Timer`/`Gauge` 使用 | 缺 tool 调用量、上游耗时、熔断状态、快照版本等业务指标 |

### P2

| 编号 | 缺口 | 实际状态（已核实） |
| --- | --- | --- |
| **SEC-03** | PIPL / GDPR 合规标注 | 未开始，无数据分类分级 |
| **OPS-02** | W3C Trace Context 注入上游 | ✅ **已闭环**（T49）：`TraceContext`（common）入站解析 + 出站注入 `traceparent`，trace-id 跨跳稳定 / span-id 每跳换新 / 非法输入不产出非法头；`X-Trace-Id` 回写实际下发的 id。契约见 `API.md §4.5` |
| **—** | PATH 变更 301 迁移提示 | 未实现，改末段即断链 |
| **MGM-05** | 审计 append-only | ✅ **已闭环**（T49）：`V8` 触发器 + REVOKE。原状态「无触发器、无 REVOKE，任何有 DB 写权限者可改历史」已消除 |
| **—** | 审计导出 | ✅ **已闭环**（T49）：`GET /audits/export`（CSV，UTF-8 BOM + 公式注入防护 + 行数上限拒绝而非截断 + 导出本身留痕），控制台带导出按钮 |
| **—** | 限流 / 配额 | **配额 ✅ 已闭环**（`scopes` → `quota`，发布时强制）；**限流评估后不做**——理由是它与配额解决的不是同一个问题，且需要令牌桶 + 协议级拒绝语义 + 快照新字段，属独立 P1 项。见 `ARCHITECTURE §4.6` |

---

## 3. 半成品清单（读写不对称，最容易被误判为"已完成"）

| 载体 | 存在的东西 | 缺失的一侧 | 后果 |
| --- | --- | --- | --- |
| `ServerUpstream.weights` | 列、实体字段、读取、executor 分发 | **写入**（`setWeights` 零调用） | ✅ 已闭环（T46） |
| `ExecutorCluster.scopes` | 列、实体、`ClusterService` 写入 | **读取**（`getScopes()` 零消费） | ✅ 已闭环（T49）：改名 `quota`，收敛为 `ClusterQuota`，发布时强制 |
| `auth_config.tool_id`（非 0 语义） | V1/V5 schema、唯一索引、`findByServerIdAndToolId` | **读写入口**（service/DTO/UI） | ✅ 已闭环（T47） |
| `ToolSnapshot.authBOverride` | 类型、executor 读取链路 | **manager 组装**（恒传 `null`） | ✅ 已闭环（T47） |
| `ResourceSnapshot` / `PromptSnapshot` | 类型、executor 全实现 | **manager 组装**（恒传空） | ✅ 已闭环（T48） |

> 共性：**executor 侧（运行时）实现得比 manager 侧（配置面）完整**。原因是数据面按"如果配置了就要能跑"的思路写全了，而配置面还没跟上。演示时若只调 executor 的单元测试，会得出"已支持"的错觉。

---

## 4. 建议排期

**第 0 步（半天，零代码风险）：** 修 §1 的 5 处文档不符。不改文档，后面排期会基于错误信息继续错。

**P1 中性价比最高的三项**（都是"另一侧已完工，只差补齐"）：

1. **resources / prompts**：executor 读端已完整，只需补 manager 侧管理模型 + UI + `SnapshotAssembler` 组装。
2. **WEIGHTED 权重**：executor 分发已完整且有测试，只需在 `UpstreamEntryRequest` 加 `weights` + 落库 + UI 编辑器。
3. **tool 级 Auth-B 覆盖**：与 REST 服务级同构（`AuthConfig` 的 `tool_id` 分支已在 schema 里），是同一个模式的第三次复用。

**需独立设计的：** EXE-07（OAuth 2.1 授权码 + PKCE + DCR）、PUB-05（需先定指标口径）。
